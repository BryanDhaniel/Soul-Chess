services:
  soul-chess:
    build:
      context: .
      dockerfile: Dockerfile
    image: soul-chess
    container_name: soul-chess
    environment:
      NODE_ENV: production
      PORT: "3000"
    ports:
      - "3000:3000"
    restart: unless-stopped
